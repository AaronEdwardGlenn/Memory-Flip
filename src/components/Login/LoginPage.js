import React, { Component } from 'react';
import useGetSpaceImages from '../../hooks/images.js';

const moonImagesRender = () => {
  const { images } = useGetSpaceImages();
};

export default class LoginPage extends Component {
  render() {
    return (
      <div>
        ij    
      </div>
    );
  }
}
